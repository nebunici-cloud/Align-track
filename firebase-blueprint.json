{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "A treatment profile (one per person/plan) tracked under a single Google account",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "planType": { "type": "string" },
        "avatarColor": { "type": "string" },
        "createdAt": { "type": "string" }
      }
    },
    "PlanMeta": {
      "title": "PlanMeta",
      "description": "Settings, maintenance tasks, notifications, and active wear timer for one treatment plan",
      "type": "object",
      "properties": {
        "settings": { "type": "object" },
        "tasks": { "type": "array" },
        "notifications": { "type": "array" },
        "activeTimer": { "type": "object" },
        "updatedAt": { "type": "string" }
      }
    },
    "WearLog": {
      "title": "WearLog",
      "description": "A single wear/out session for aligner tracking",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "date": { "type": "string" },
        "type": { "type": "string" },
        "startTime": { "type": "string" },
        "endTime": { "type": "string" },
        "durationMinutes": { "type": "number" },
        "reason": { "type": "string" },
        "notes": { "type": "string" }
      }
    },
    "PhotoEntry": {
      "title": "PhotoEntry",
      "description": "Smile diary photo metadata; the image itself lives in Firebase Storage",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "trayNumber": { "type": "number" },
        "date": { "type": "string" },
        "imageUrl": { "type": "string" },
        "note": { "type": "string" }
      }
    }
  },
  "firestore": {
    "/users/{userId}/profile/main": {
      "description": "The account list (UserProfile[]) and currently selected accountId for this Google account"
    },
    "/users/{userId}/plans/{accountId}/meta/main": {
      "schema": "PlanMeta",
      "description": "Bounded per-plan document: settings, tasks, notifications, active timer"
    },
    "/users/{userId}/plans/{accountId}/wearLogs/{logId}": {
      "schema": "WearLog",
      "description": "One document per wear/out session; unbounded, ordered by startTime"
    },
    "/users/{userId}/plans/{accountId}/photos/{photoId}": {
      "schema": "PhotoEntry",
      "description": "One document per smile-diary photo; imageUrl points to Firebase Storage"
    }
  },
  "storage": {
    "/users/{userId}/plans/{accountId}/photos/{photoId}": {
      "description": "Smile diary image files, uploaded directly rather than embedded as base64 in Firestore"
    }
  }
}
